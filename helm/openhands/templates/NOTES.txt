🎉 Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ include "openhands.namespace" . }}
  $ helm get all {{ .Release.Name }} -n {{ include "openhands.namespace" . }}

═══════════════════════════════════════════════════════════════

📋 GETTING STARTED:

1. Watch the deployment rollout status:

   kubectl rollout status deployment/{{ include "openhands.fullname" . }} -n {{ include "openhands.namespace" . }}

2. Get the application URL by running these commands:

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  🌐 http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ include "openhands.namespace" . }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "openhands.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ include "openhands.namespace" . }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "🌐 OpenHands is available at http://$NODE_IP:$NODE_PORT"
{{- else if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        You can watch the status by running:
        
        kubectl get --namespace {{ include "openhands.namespace" . }} svc -w {{ include "openhands.fullname" . }}

  export SERVICE_IP=$(kubectl get svc --namespace {{ include "openhands.namespace" . }} {{ include "openhands.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo "🌐 OpenHands is available at http://$SERVICE_IP:{{ .Values.service.port }}"
{{- else if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ include "openhands.namespace" . }} -l "app.kubernetes.io/name={{ include "openhands.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ include "openhands.namespace" . }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "🌐 Visit http://127.0.0.1:3000 to use OpenHands"
  kubectl --namespace {{ include "openhands.namespace" . }} port-forward $POD_NAME 3000:$CONTAINER_PORT
{{- end }}

═══════════════════════════════════════════════════════════════

⚙️  CONFIGURATION:

{{- if not .Values.config.llm.apiKey }}
⚠️  WARNING: No LLM API key configured!
   
   OpenHands requires an LLM API key to function. You can set it by:
   
   1. Upgrading the release with --set config.llm.apiKey=<your-api-key>
   2. Or by updating the secret manually:
   
      kubectl create secret generic {{ include "openhands.fullname" . }} \
        --from-literal=llm-api-key=<your-api-key> \
        --namespace {{ include "openhands.namespace" . }} \
        --dry-run=client -o yaml | kubectl apply -f -
{{- end }}

Current configuration:
  • LLM Model: {{ .Values.config.llm.model }}
  • Default Agent: {{ .Values.config.core.defaultAgent }}
  • Max Iterations: {{ .Values.config.core.maxIterations }}
  • Workspace Storage: {{ if .Values.persistence.enabled }}{{ .Values.persistence.size }} (PVC){{ else }}ephemeral{{ end }}
  • File Store: {{ if .Values.fileStore.enabled }}{{ .Values.fileStore.size }} (PVC){{ else }}disabled{{ end }}

═══════════════════════════════════════════════════════════════

📚 DOCUMENTATION & SUPPORT:

  • Documentation: https://docs.openhands.dev
  • GitHub: https://github.com/OpenHands/OpenHands
  • Slack Community: https://dub.sh/openhands

═══════════════════════════════════════════════════════════════

🔧 TROUBLESHOOTING:

View logs:
  kubectl logs -f deployment/{{ include "openhands.fullname" . }} -n {{ include "openhands.namespace" . }}

Check pod status:
  kubectl get pods -n {{ include "openhands.namespace" . }} -l app.kubernetes.io/instance={{ .Release.Name }}

Describe the deployment:
  kubectl describe deployment {{ include "openhands.fullname" . }} -n {{ include "openhands.namespace" . }}

═══════════════════════════════════════════════════════════════

Happy coding with OpenHands! 🚀
